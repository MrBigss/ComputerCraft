-- Put on a floopy disk, and rename as startup.lua, in root.
-- Then when put in a drive next to a turtle, this will set it up, and/or update it.

shell.run("pastebin run p8PJVxC4")
shell.run("github clone MrBigss/ComputerCraft -b main")
